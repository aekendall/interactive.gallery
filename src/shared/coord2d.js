(function(exports) {

    var RAD_TO_DEG = 57.2957795131;

    /*
     function Coord2d(x, y) {
     if (typeof x === 'object') {
     this.x = x.x;
     this.y = x.y;
     } else {
     this.x = x || 0;
     this.y = y || 0;
     }
     }
     */

    /*
    function get() {

    }

    exports.get = get;
    */
})(typeof exports === 'undefined' ? this['Coord2d'] = {} : exports);